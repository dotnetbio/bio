<!-- 
<summary>
    // Progress bar will display the progress of: 
    // 1. Parsing the sequences stored in GenBank and Fasta files.
    // 2. Assembly process.
    // 3. Web service execution process.
</summary> 
-->
    <UserControl x:Class="SequenceAssembler.ProgressBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Height="300" Width="300" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">
	
	<!-- Displays the progress bar control -->
	<Grid Margin="8,0,8,0" >
		<StackPanel d:LayoutOverrides="Height">

			<!-- Heading for progress bar control -->
			<TextBlock Foreground="#FF4D4D4D" TextWrapping="Wrap" FontWeight="Bold" FontSize="12"><Run Text=""/><Run FontWeight="Normal" Text=""/></TextBlock>
			
			<!-- Display progress bar button and cancel button -->
			<DockPanel>
				<Button Content="Cancel" Margin="5,0,0,0" DockPanel.Dock="Right" FontSize="12"/>
				<ProgressBar Height="24" Value="30"/>
			</DockPanel>
		</StackPanel>
		
	</Grid> 
</UserControl>
